Manager for 3D Print files

I can never find what I downloaded, so this shows my prints visually and has features so you can search prints and label them (if a print doesn't work exactly as you planned).

It should place a folder in your downloads folder so you can download to that folder and it will automatcially archive...where the installer is?

Use installer.bat to install it and run.bat to run (installer will prompt you)

the system tray icon will display a blue icon for a fast shortcut to the page as well as the ability to kill the server (or so i thought; just press a key on the command prompt to kill the server).